<!--
Generated by /learn-this-project-meta on 2026-05-13.
Source of truth: this repository at commit 2cd4f81.
Edit this file directly when you spot inaccuracies. Re-run the meta-skill with
`refresh` to regenerate, or `refresh <name>` to regenerate just one.
-->

# Quiz Bank — `smb_loan_ai`

Granular Q&A items used by `/learn-this-project-quiz`. Each item fits in ≤ 2 sentences. Tags: `knowhow` (the why), `runbook` (how to run/test/debug), `convention` (project patterns), `deps` (external libs).

---

- **Q-001** [tag: knowhow] [difficulty: easy]
  - **Q:** What single import gives you access to the entire backend's capabilities, and what does it return?
  - **A:** `from smb_loan_ai.api import one` — returns the `One` singleton with config, DB engines, AWS clients, and the Strands agent all wired up.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#1-singleton-entry--top-level-glue-smb_loan_ai`

- **Q-002** [tag: knowhow] [difficulty: medium]
  - **Q:** Why does `One` use multiple inheritance from four mixins instead of being a single class?
  - **A:** Each mixin owns one capability (config, db, AWS, agent) and lives in its own file (`one_01_…` through `one_04_…`) so it's individually understandable and testable. Composition over a thousand-line god-object.
  - **Source:** `smb_loan_ai/one/one_00_main.py`

- **Q-003** [tag: convention] [difficulty: easy]
  - **Q:** What does the `_00_` prefix in `one_00_main.py` and `conf_00_def.py` indicate?
  - **A:** Reading/load order — lower numbers are read first. Convention for new contributors to navigate the package.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#conventions`

- **Q-004** [tag: knowhow] [difficulty: medium]
  - **Q:** How does the code decide between SQLite and Postgres at runtime?
  - **A:** `smb_loan_ai/runtime.py` checks the `VERCEL` env var; `DbMixin.engine` returns `local_sqlite_engine` if `is_local()` else `remote_postgres_engine`.
  - **Source:** `smb_loan_ai/one/one_02_db.py`, `smb_loan_ai/runtime.py`

- **Q-005** [tag: knowhow] [difficulty: medium]
  - **Q:** Why is the agent's system prompt stored as a `.md` file rather than an inline Python string?
  - **A:** So non-developers can edit it without touching code and so prompt changes diff cleanly in PRs.
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-006** [tag: knowhow] [difficulty: hard]
  - **Q:** Why is SQL tool output formatted as Markdown tables instead of JSON?
  - **A:** Markdown tables are roughly 24% more token-efficient than equivalent JSON when the LLM reads tool results.
  - **Source:** `smb_loan_ai/sql_utils.py`, `docs/learn-this-project/01-knowhow-inventory.md#conventions`

- **Q-007** [tag: deps] [difficulty: easy]
  - **Q:** What agent framework powers the backend?
  - **A:** `strands-agents` — provides the `Agent` class, the `@tool` decorator, and `BedrockModel`.
  - **Source:** `pyproject.toml`, `smb_loan_ai/one/one_04_agent.py`

- **Q-008** [tag: deps] [difficulty: medium]
  - **Q:** Why Strands over LangChain or Bedrock Agents?
  - **A:** Strands is lighter, Bedrock-native, and not AWS-side-configured. LangChain would be ~3× boilerplate for the three tools used here; Bedrock Agents would couple us to AWS console state.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#external-dependencies--the-non-obvious-ones`

- **Q-009** [tag: knowhow] [difficulty: medium]
  - **Q:** What three tools does the agent expose?
  - **A:** `tool_get_database_schema`, `tool_execute_sql_query`, `tool_write_debug_report`.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`

- **Q-010** [tag: knowhow] [difficulty: medium]
  - **Q:** Which tool does the system prompt instruct the agent to call FIRST and why?
  - **A:** `tool_get_database_schema`, so the agent knows the available tables and columns before writing any SQL.
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-011** [tag: knowhow] [difficulty: hard]
  - **Q:** What stops the agent from running `DROP TABLE customer`?
  - **A:** `sql_utils.ensure_valid_select_query` rejects anything not starting with `SELECT`; the system prompt also explicitly forbids write operations. Defense-in-depth would add a read-only DB user.
  - **Source:** `smb_loan_ai/sql_utils.py`, `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-012** [tag: runbook] [difficulty: easy]
  - **Q:** What command starts both the Next.js and FastAPI dev servers at once?
  - **A:** `mise run dev` — also kills stale servers first via `mise run kill`.
  - **Source:** `docs/learn-this-project/02-runbook.md#day-to-day-commands`

- **Q-013** [tag: runbook] [difficulty: easy]
  - **Q:** What URLs do the chat UI and FastAPI health check live at locally?
  - **A:** Chat: `http://localhost:3000/chat`. Health: `http://localhost:8000/api/hello`.
  - **Source:** `docs/learn-this-project/02-runbook.md#running-locally`

- **Q-014** [tag: runbook] [difficulty: medium]
  - **Q:** Where does the local SQLite database come from?
  - **A:** `mise run download-db` (= `scripts/test_download_sqlite_file.py`) pulls a snapshot from a GitHub Release into `tmp/data.sqlite`.
  - **Source:** `docs/learn-this-project/02-runbook.md#first-time-setup`

- **Q-015** [tag: convention] [difficulty: easy]
  - **Q:** Where are file paths defined in this project?
  - **A:** `smb_loan_ai/paths.py` exposes a `path_enum` singleton. All code reads paths via `path_enum.path_*` rather than hardcoded strings.
  - **Source:** `smb_loan_ai/paths.py`

- **Q-016** [tag: convention] [difficulty: medium]
  - **Q:** What's the difference between functions in `sql_utils.py` and methods on `DbMixin`?
  - **A:** Pure functions take `engine` as first argument and have no class state; mixin methods read `self.engine`. Use pure functions in scripts/tests, mixin methods inside the agent flow.
  - **Source:** `smb_loan_ai/sql_utils.py`, `smb_loan_ai/one/one_02_db.py`

- **Q-017** [tag: knowhow] [difficulty: hard]
  - **Q:** Why is the LLM-optimized schema string roughly 70% smaller than raw SQL DDL?
  - **A:** It uses single-letter type abbreviations (`STR`/`INT`/`DEC`/`TS`/`DT`) and compact markers (`*PK`, `*FK→Table.Column`) instead of full SQL syntax — minimizes tokens since the schema rides along on every agent request.
  - **Source:** `smb_loan_ai/db_schema/encoder.py`

- **Q-018** [tag: deps] [difficulty: medium]
  - **Q:** What does `vercel-ai-sdk-mate` do for the backend?
  - **A:** Parses the Vercel AI SDK message format (`RequestBody`, `MessagePartTypeEnum`) so the FastAPI side can read what `@ai-sdk/react`'s `useChat` sends.
  - **Source:** `smb_loan_ai/ai_sdk_adapter.py`, `pyproject.toml`

- **Q-019** [tag: knowhow] [difficulty: medium]
  - **Q:** What does `smb_loan_ai/ai_sdk_adapter.py` do?
  - **A:** Translates between Vercel AI SDK message format (frontend) and AWS Bedrock Converse format (backend), and emits the SSE stream with reasoning blocks the frontend `useChat` consumes.
  - **Source:** `smb_loan_ai/ai_sdk_adapter.py`

- **Q-020** [tag: knowhow] [difficulty: medium]
  - **Q:** Why does this project have its own `ai_sdk_adapter.py` instead of using a library?
  - **A:** The translation needed is small and project-specific; a general-purpose adapter would be more code and one more dependency to track for protocol drift.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#smb_loan_aiai_sdk_adapterpy`

- **Q-021** [tag: runbook] [difficulty: easy]
  - **Q:** How do you run only the Python tests?
  - **A:** `mise run test-python` — runs `pytest tests_python`.
  - **Source:** `docs/learn-this-project/02-runbook.md#day-to-day-commands`

- **Q-022** [tag: runbook] [difficulty: medium]
  - **Q:** Where does the pytest coverage HTML report end up, and how do you generate it?
  - **A:** `mise run cov-python` → `htmlcov/index.html`. The directory is gitignored — never commit it.
  - **Source:** `docs/learn-this-project/02-runbook.md#day-to-day-commands`

- **Q-023** [tag: knowhow] [difficulty: hard]
  - **Q:** Why is the schema reflection result cached via `@cached_property` on `DbMixin.database_schema_str`?
  - **A:** Reflection runs `MetaData.reflect()` which is expensive; the schema doesn't change at runtime so it's safe to compute once per process.
  - **Source:** `smb_loan_ai/one/one_02_db.py`

- **Q-024** [tag: knowhow] [difficulty: medium]
  - **Q:** What happens during the `/api/chat` request handler, in order?
  - **A:** Parse Vercel AI SDK request → check quota → restore agent history → invoke `one.agent` → extract reasoning/answer via `agent_debugger` → stream back via SSE.
  - **Source:** `api/index.py`, `smb_loan_ai/ai_sdk_adapter.py`

- **Q-025** [tag: knowhow] [difficulty: medium]
  - **Q:** What does `smb_loan_ai/quota.py` do and why is it there?
  - **A:** Tracks per-user monthly input/output token usage in DynamoDB and raises `QuotaExceededError` past the cap, preventing runaway Bedrock costs on this public demo.
  - **Source:** `smb_loan_ai/quota.py`

- **Q-026** [tag: knowhow] [difficulty: easy]
  - **Q:** How does the frontend stream the agent's reasoning to the user?
  - **A:** `@ai-sdk/react`'s `useChat()` hook consumes the SSE stream from `/api/chat`; the stream contains separate reasoning and text frames.
  - **Source:** `components/chat/chat.tsx`, `smb_loan_ai/ai_sdk_adapter.py`

- **Q-027** [tag: convention] [difficulty: easy]
  - **Q:** Where does shadcn/ui live in this project?
  - **A:** `components/ui/` — about 50 copy-into-repo wrappers around `@radix-ui/*` primitives.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#componentsui`

- **Q-028** [tag: convention] [difficulty: medium]
  - **Q:** Why does `app/(marketing)/` have parentheses around `marketing`?
  - **A:** Next.js route-group syntax — the segment doesn't appear in the URL but lets the marketing pages share a layout.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#app`

- **Q-029** [tag: convention] [difficulty: medium]
  - **Q:** Why is there no `app/api/` directory?
  - **A:** The backend is the separate FastAPI process at `api/index.py`, not Next.js API routes — keeps Python and TypeScript responsibilities separate.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#app`

- **Q-030** [tag: runbook] [difficulty: easy]
  - **Q:** What command kills stale dev servers?
  - **A:** `mise run kill` — runs `kill-dev-servers.py` which clears ports 3000/3001/3002/8000/8080.
  - **Source:** `docs/learn-this-project/02-runbook.md#day-to-day-commands`

- **Q-031** [tag: deps] [difficulty: medium]
  - **Q:** What library handles AWS credential resolution for both local `~/.aws/credentials` and Vercel env vars?
  - **A:** `boto-session-manager` — wraps `boto3.Session` to centralize credential discovery.
  - **Source:** `pyproject.toml`, `smb_loan_ai/one/one_03_boto3.py`

- **Q-032** [tag: deps] [difficulty: easy]
  - **Q:** Which library renders SQL results as Markdown tables?
  - **A:** `tabulate`, called from `sql_utils.format_result`.
  - **Source:** `smb_loan_ai/sql_utils.py`

- **Q-033** [tag: knowhow] [difficulty: medium]
  - **Q:** What's `DEFAULT_MAX_ROWS` set to in `sql_utils.py` and why does it exist?
  - **A:** 200 rows. Caps the result size so a runaway SELECT doesn't blow up the LLM context or the SSE frame budget.
  - **Source:** `smb_loan_ai/sql_utils.py`

- **Q-034** [tag: knowhow] [difficulty: medium]
  - **Q:** What's `DEFAULT_MAX_CHARS` and what does it guard?
  - **A:** 20000 characters — second cap on result size to protect token budget even if rows are wide.
  - **Source:** `smb_loan_ai/sql_utils.py`

- **Q-035** [tag: knowhow] [difficulty: hard]
  - **Q:** What's the bug with `tool_write_debug_report`?
  - **A:** It references `path_enum.path_debug_report_md` but that attribute is not defined in `paths.py` — the tool will raise `AttributeError` when invoked.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`, `smb_loan_ai/paths.py`

- **Q-036** [tag: runbook] [difficulty: medium]
  - **Q:** How do you test the agent end-to-end without spinning up the frontend?
  - **A:** `.venv/bin/python scripts/test_agent.py` — invokes the agent with a hard-coded question.
  - **Source:** `docs/learn-this-project/02-runbook.md#ad-hoc-utilities`

- **Q-037** [tag: runbook] [difficulty: medium]
  - **Q:** How do you run a SQL SELECT against the local SQLite from the CLI?
  - **A:** `.venv/bin/python scripts/test_run_sql_locally_cli.py --sql "SELECT * FROM customer LIMIT 5"`.
  - **Source:** `docs/learn-this-project/02-runbook.md#ad-hoc-utilities`

- **Q-038** [tag: runbook] [difficulty: medium]
  - **Q:** How do you print the LLM-optimized schema text?
  - **A:** `.venv/bin/python scripts/test_print_database_schema.py`.
  - **Source:** `docs/learn-this-project/02-runbook.md#ad-hoc-utilities`

- **Q-039** [tag: convention] [difficulty: hard]
  - **Q:** Why is the test file name `test_one_one_02_db.py` with two `one`s?
  - **A:** Tests mirror source paths and prefix with `test_`; the source is `smb_loan_ai/one/one_02_db.py`, so the test ends up as `test_one_one_02_db.py`. Unusual-looking but consistent.
  - **Source:** `tests_python/one/test_one_one_02_db.py`

- **Q-040** [tag: deps] [difficulty: easy]
  - **Q:** What charting library is in `package.json`?
  - **A:** `recharts` — installed but not yet used; likely staged for visualizing SQL results as charts.
  - **Source:** `package.json`, `docs/learn-this-project/03-elevation-roadmap.md#projects-not-included-hypotheses`

- **Q-041** [tag: knowhow] [difficulty: medium]
  - **Q:** What does `agent_debugger.py` do?
  - **A:** Parses the Strands agent's message history to separate `<thinking>…</thinking>` blocks from the final answer, so they stream as different SSE frame types.
  - **Source:** `smb_loan_ai/agent_debugger.py`

- **Q-042** [tag: convention] [difficulty: easy]
  - **Q:** What Python version is pinned for this project?
  - **A:** 3.12 (in both `mise.toml` and `pyproject.toml`'s `requires-python = ">=3.12,<4.0"`).
  - **Source:** `mise.toml`, `pyproject.toml`

- **Q-043** [tag: convention] [difficulty: easy]
  - **Q:** What Node version and package manager are pinned?
  - **A:** Node 24 (via `mise.toml`); pnpm 10.33.3 (via `package.json`'s `packageManager`).
  - **Source:** `mise.toml`, `package.json`

- **Q-044** [tag: knowhow] [difficulty: medium]
  - **Q:** What does `runtime.py` use to remember the runtime mode across calls?
  - **A:** `@cached_property` on `name`, so the `VERCEL` env var is read once per process.
  - **Source:** `smb_loan_ai/runtime.py`

- **Q-045** [tag: knowhow] [difficulty: hard]
  - **Q:** Why does `Config` have two factory classmethods instead of one constructor?
  - **A:** `new_in_local_runtime()` reads from `~/.aws/credentials` and `.env`; `new_in_vercel_runtime()` reads from `os.environ`. Different sources, same target object — factories avoid `if runtime.is_local()` checks scattered through field assignments.
  - **Source:** `smb_loan_ai/config/conf_00_def.py`

- **Q-046** [tag: knowhow] [difficulty: medium]
  - **Q:** Which AWS region does this project use, and where is that pinned?
  - **A:** `us-east-1`. Set in `Config.aws_region` and required for the Bedrock model id (`us.amazon.nova-micro-v1:0`).
  - **Source:** `smb_loan_ai/config/conf_00_def.py`

- **Q-047** [tag: knowhow] [difficulty: medium]
  - **Q:** Name two of the 13 derived BI metrics the system prompt teaches the agent.
  - **A:** Any two of: default rate, pricing gap, approval rate, false rejection rate, late payment rate, partial payment percentage, recovery rate, loss severity, expected loss, portfolio concentration, payment cohorts, customer LTV, vintage analysis.
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-048** [tag: knowhow] [difficulty: medium]
  - **Q:** What's the customer-side lifecycle the system prompt encodes?
  - **A:** Customer → Application → Loan → Payments and Default_Event.
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-049** [tag: knowhow] [difficulty: hard]
  - **Q:** Why does the system prompt include the line "force float division" for SQLite?
  - **A:** SQLite integer division truncates (e.g., `5/2 = 2`); BI metrics need percentages, so the prompt teaches the agent to multiply by `1.0` (or 100.0) to coerce to float.
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-050** [tag: convention] [difficulty: medium]
  - **Q:** Where does the agent write its debug reasoning trace?
  - **A:** `tmp/debug_report.md`, via `tool_write_debug_report`. (Note: the path is currently underdefined; see `Q-035`.)
  - **Source:** `smb_loan_ai/one/one_04_agent.py`

- **Q-051** [tag: runbook] [difficulty: medium]
  - **Q:** How does Vercel know which files to deploy as Serverless Functions?
  - **A:** `vercel.json` declares `api/**` as functions and pins `PYTHON_VERSION=3.12`.
  - **Source:** `vercel.json`

- **Q-052** [tag: knowhow] [difficulty: medium]
  - **Q:** Why does `boto-session-manager` exist in this project, given `boto3.Session` already exists?
  - **A:** It centralizes credential resolution across local `~/.aws/credentials` and Vercel env vars, and pairs with `pynamodb-session-manager` for the quota table.
  - **Source:** `smb_loan_ai/one/one_03_boto3.py`, `smb_loan_ai/quota.py`

- **Q-053** [tag: deps] [difficulty: medium]
  - **Q:** What does `boto3-dataclass[bedrock-runtime]` give you?
  - **A:** Type-safe dataclass wrappers for Bedrock response dictionaries, so you get IDE autocomplete instead of `response["output"]["message"]["content"]`.
  - **Source:** `pyproject.toml`

- **Q-054** [tag: knowhow] [difficulty: easy]
  - **Q:** What's the user-facing chat URL?
  - **A:** `http://localhost:3000/chat` locally; production URL is the Vercel deployment URL `+ /chat`.
  - **Source:** `app/chat/page.tsx`

- **Q-055** [tag: convention] [difficulty: medium]
  - **Q:** Where is `useChat` used and what does it do?
  - **A:** `components/chat/chat.tsx` calls `useChat()` from `@ai-sdk/react` — manages messages, sends to `/api/chat`, parses the SSE stream.
  - **Source:** `components/chat/chat.tsx`

- **Q-056** [tag: knowhow] [difficulty: hard]
  - **Q:** If you edit `bi-agent-system-prompt.md`, why might your change not take effect?
  - **A:** The prompt is loaded via `@cached_property` once per process — you must restart the FastAPI server to pick up edits.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`

- **Q-057** [tag: knowhow] [difficulty: medium]
  - **Q:** What sort of file is `smb_loan_ai/multi_round_bedrock_runtime_chat_manager.py` and what's its status?
  - **A:** Legacy pre-Strands chat manager. Not imported by the current code path — effectively dead code.
  - **Source:** `smb_loan_ai/multi_round_bedrock_runtime_chat_manager.py`

- **Q-058** [tag: convention] [difficulty: easy]
  - **Q:** What command exports the current Python deps to `requirements.txt`?
  - **A:** `mise run export`.
  - **Source:** `mise.toml`

- **Q-059** [tag: knowhow] [difficulty: medium]
  - **Q:** What does the `data/database-exploration/` directory contain?
  - **A:** `database-schema.txt` (read-only schema reference) and `suggested-actions.json` (the quick-action chips shown on the chat page).
  - **Source:** `data/database-exploration/`

- **Q-060** [tag: knowhow] [difficulty: hard]
  - **Q:** Why is the schema reflected at runtime rather than committed as a static file?
  - **A:** Single source of truth: whatever the DB says is the schema. Committing it would risk drift between the live DB and the file the agent sees.
  - **Source:** `smb_loan_ai/one/one_02_db.py`

- **Q-061** [tag: runbook] [difficulty: medium]
  - **Q:** How would you turn on SQL query logging in development?
  - **A:** Pass `echo=True` to the `create_engine` call inside `smb_loan_ai/one/one_02_db.py`; SQLAlchemy will then log every query to stdout.
  - **Source:** `smb_loan_ai/one/one_02_db.py`

- **Q-062** [tag: knowhow] [difficulty: medium]
  - **Q:** What's the relationship between `smb_loan_ai/api.py` and `smb_loan_ai/one/api.py`?
  - **A:** `smb_loan_ai/one/api.py` constructs `one = One()`; `smb_loan_ai/api.py` re-exports it as the top-level import path. Two files, one chain.
  - **Source:** `smb_loan_ai/api.py`, `smb_loan_ai/one/api.py`

- **Q-063** [tag: knowhow] [difficulty: hard]
  - **Q:** Why is the agent's input quota tracked at the user level, not the global level?
  - **A:** A global quota means one heavy user can lock out everyone else; per-user lets each visitor get their share and only that user gets blocked when over limit.
  - **Source:** `smb_loan_ai/quota.py`

- **Q-064** [tag: convention] [difficulty: medium]
  - **Q:** What's the structure of a typical agent tool, in one sentence?
  - **A:** A method on `AgentMixin` decorated with `@tool`, typed signature, docstring describing when to call it, registered in `agent.tools=[…]`.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`

- **Q-065** [tag: runbook] [difficulty: medium]
  - **Q:** What's the first thing to check if `mise run dev` reports a port-in-use error?
  - **A:** Run `mise run kill` — `kill-dev-servers.py` frees the dev ports cleanly.
  - **Source:** `docs/learn-this-project/02-runbook.md#common-failures-and-fixes`

- **Q-066** [tag: runbook] [difficulty: easy]
  - **Q:** What's the FastAPI health-check endpoint?
  - **A:** `GET /api/hello`.
  - **Source:** `api/index.py`

- **Q-067** [tag: convention] [difficulty: easy]
  - **Q:** Where does the chat page live in `app/`?
  - **A:** `app/chat/page.tsx`.
  - **Source:** `app/chat/page.tsx`

- **Q-068** [tag: knowhow] [difficulty: medium]
  - **Q:** Why does `package.json` have a `fastapi-dev` script if `mise.toml` also has one?
  - **A:** The `package.json` script predates the `mise.toml` versions and is legacy — `mise run fastapi-dev` is the canonical entry point now.
  - **Source:** `package.json`, `mise.toml`

- **Q-069** [tag: knowhow] [difficulty: hard]
  - **Q:** What's the major risk of `Bedrock prompt caching` for this project if added?
  - **A:** Cache invalidation: if the schema changes mid-session, cached prompt entries become stale and the agent may use an outdated schema for some users.
  - **Source:** `docs/learn-this-project/03-elevation-roadmap.md#9-performance--scalability`

- **Q-070** [tag: knowhow] [difficulty: medium]
  - **Q:** Why does `tool_execute_sql_query` only accept SELECT statements?
  - **A:** Read-only safety constraint — prevents the agent from accidentally (or via prompt injection) issuing UPDATE/INSERT/DELETE/DROP. Validated by `ensure_valid_select_query`.
  - **Source:** `smb_loan_ai/sql_utils.py`

- **Q-071** [tag: deps] [difficulty: medium]
  - **Q:** What does `framer-motion` provide on the frontend?
  - **A:** Animation primitives — likely used for chat-message enter/exit transitions and any landing-page motion.
  - **Source:** `package.json`

- **Q-072** [tag: deps] [difficulty: easy]
  - **Q:** Which library is used for toast notifications on the frontend?
  - **A:** `sonner`.
  - **Source:** `package.json`

- **Q-073** [tag: convention] [difficulty: medium]
  - **Q:** What pattern is `lib/utils.ts`'s `cn()` helper using, and what does it do?
  - **A:** The standard shadcn pattern: `clsx` + `tailwind-merge`. Combines conditional class strings and resolves Tailwind conflicts (last-write-wins).
  - **Source:** `lib/utils.ts`

- **Q-074** [tag: knowhow] [difficulty: medium]
  - **Q:** What does `data/suggested-actions.json` drive?
  - **A:** The quick-action chips on the chat page — pre-baked example questions for the user.
  - **Source:** `data/suggested-actions.json`, `components/chat/`

- **Q-075** [tag: knowhow] [difficulty: hard]
  - **Q:** If two users hit `/api/chat` simultaneously, can they share `one`'s state and cause cross-talk?
  - **A:** `one` is a module-level singleton with shared resources (engine, agent). The Strands `Agent` itself is stateful in the message history sense, so message-history must be reconstructed per-request from the AI SDK request body — which `api/index.py` does explicitly before invoking.
  - **Source:** `api/index.py`, `smb_loan_ai/one/api.py`

- **Q-076** [tag: convention] [difficulty: medium]
  - **Q:** What's the unique twist of this project's schema-encoding format?
  - **A:** Compact single-letter type codes (`STR`/`INT`/`DEC`/`TS`/`DT`) and constraint markers (`*PK`, `*FK→Table.Column`, `*UQ`, `*NN`) — saves ~70% tokens vs raw SQL DDL.
  - **Source:** `smb_loan_ai/db_schema/encoder.py`

- **Q-077** [tag: runbook] [difficulty: medium]
  - **Q:** What environment variable distinguishes the Vercel runtime from local?
  - **A:** `VERCEL=1` — set automatically by Vercel; checked by `runtime.py`.
  - **Source:** `smb_loan_ai/runtime.py`

- **Q-078** [tag: deps] [difficulty: easy]
  - **Q:** What CLI argument framework is used in `scripts/`?
  - **A:** `fire` — reads function signatures to build the CLI.
  - **Source:** `pyproject.toml`, `scripts/`

- **Q-079** [tag: knowhow] [difficulty: medium]
  - **Q:** Where does the agent's system prompt explicitly mention SQLite vs Postgres handling?
  - **A:** `smb_loan_ai/prompts/bi-agent-system-prompt.md` includes SQLite-specific guidance (integer division, `strftime`) because the local engine is SQLite.
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-080** [tag: convention] [difficulty: medium]
  - **Q:** What's the directory structure mirror rule for tests?
  - **A:** `smb_loan_ai/<path>/<file>.py` is tested by `tests_python/<path>/test_<file>.py` — paths mirror, files prefix with `test_`.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#conventions`

- **Q-081** [tag: knowhow] [difficulty: hard]
  - **Q:** Why might you want a "read-only DB user" as a hardening step, even if the SELECT validator already exists?
  - **A:** Defense in depth — a string-level validator can be tricked by clever payloads or comment tricks; a database-level `GRANT SELECT` only is bullet-proof.
  - **Source:** `docs/learn-this-project/03-elevation-roadmap.md#4-security--input-safety`

- **Q-082** [tag: deps] [difficulty: medium]
  - **Q:** Which library converts function arguments into a runtime-inspectable shape, useful for tool registration?
  - **A:** `func-args` — listed in `pyproject.toml`; helps the agent framework understand tool signatures.
  - **Source:** `pyproject.toml`

- **Q-083** [tag: knowhow] [difficulty: medium]
  - **Q:** Why isn't there a backend test for `/api/chat` today?
  - **A:** The integration would need either a mocked Bedrock or a real call; both are non-trivial — it's been deferred. See the test-depth elevation item.
  - **Source:** `docs/learn-this-project/03-elevation-roadmap.md#2-test-depth-and-coverage`

- **Q-084** [tag: knowhow] [difficulty: hard]
  - **Q:** What does an "eval set" buy you that integration tests don't, for an LLM-backed app?
  - **A:** Tests check deterministic behavior; evals score *generative* quality (did the agent pick the right SQL shape?). Evals catch prompt regressions that pass type-checked tests.
  - **Source:** `docs/learn-this-project/03-elevation-roadmap.md#3-agent-evaluation-harness`

- **Q-085** [tag: convention] [difficulty: medium]
  - **Q:** What's the convention for adding a new agent tool, end-to-end?
  - **A:** Define a `@tool` method on `AgentMixin` in `one_04_agent.py`, register it in `agent.tools=[…]`, then update the system prompt so the LLM knows when to call it.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`

- **Q-086** [tag: runbook] [difficulty: easy]
  - **Q:** Which command installs both Python and Node deps at once?
  - **A:** `mise run inst`.
  - **Source:** `mise.toml`

- **Q-087** [tag: knowhow] [difficulty: medium]
  - **Q:** What does `blogs/` contain, and why shouldn't it be shown in a demo?
  - **A:** Personal teaching/learning essays by the project owner. Not product documentation — showing it makes the project look unfocused.
  - **Source:** `docs/learn-this-project/06-demo-playbook.md#do-not-show--explicit-list`

- **Q-088** [tag: knowhow] [difficulty: medium]
  - **Q:** Why are `scripts/test_ai_glm.py` and `scripts/test_ai_google_gemini.py` kept around?
  - **A:** Best guess: provider-exploration sketches in case the project ever swaps Bedrock for another LLM. Not production code; verify with project owner before relying on them.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#9-scripts-scripts`

- **Q-089** [tag: convention] [difficulty: easy]
  - **Q:** What's the package name in `pyproject.toml`?
  - **A:** `smb_loan_ai`.
  - **Source:** `pyproject.toml`

- **Q-090** [tag: convention] [difficulty: easy]
  - **Q:** What's the npm package name in `package.json`?
  - **A:** `smb-loan-ai` (note: hyphen, not underscore — Node convention).
  - **Source:** `package.json`

- **Q-091** [tag: knowhow] [difficulty: hard]
  - **Q:** If you wanted Bedrock prompt caching, where would you wire it?
  - **A:** Inside `smb_loan_ai/one/one_04_agent.py`'s `BedrockModel` construction, passing the system prompt + schema as cached blocks. Strands exposes this via `BedrockModel` kwargs.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`, `docs/learn-this-project/03-elevation-roadmap.md#9-performance--scalability`

- **Q-092** [tag: runbook] [difficulty: medium]
  - **Q:** What's the minimal `.env` you need for purely-local SQLite work?
  - **A:** None — `.env` is only needed for Postgres credentials. SQLite mode (default locally) works without it.
  - **Source:** `docs/learn-this-project/02-runbook.md#first-time-setup`

- **Q-093** [tag: knowhow] [difficulty: medium]
  - **Q:** What runtime-detection question does every environment-aware file in the codebase boil down to?
  - **A:** `runtime.is_local()` vs `runtime.is_vercel()` — checked via the `VERCEL` env var. Centralized in `runtime.py`.
  - **Source:** `smb_loan_ai/runtime.py`

- **Q-094** [tag: knowhow] [difficulty: hard]
  - **Q:** Why a Postgres pool size of 1 per Lambda instance in production?
  - **A:** Each Vercel Serverless Function invocation runs in its own container with low concurrency; pool > 1 wastes connections and risks NeonDB connection limits.
  - **Source:** `docs/learn-this-project/03-elevation-roadmap.md#9-performance--scalability`

- **Q-095** [tag: deps] [difficulty: medium]
  - **Q:** Which library provides `useChat`'s React-side counterpart that the Vercel AI SDK builds on?
  - **A:** `@ai-sdk/react` — the React hooks package; `ai` is the lower-level core library.
  - **Source:** `package.json`

- **Q-096** [tag: knowhow] [difficulty: medium]
  - **Q:** What does the system prompt say the agent should do AFTER answering the user's question?
  - **A:** Call `tool_write_debug_report` to write the reasoning trace to `tmp/debug_report.md`. (See `Q-035` for the current bug.)
  - **Source:** `smb_loan_ai/prompts/bi-agent-system-prompt.md`

- **Q-097** [tag: convention] [difficulty: easy]
  - **Q:** Which file lists ALL `mise run` tasks?
  - **A:** `mise.toml` (the `[tasks]` section).
  - **Source:** `mise.toml`

- **Q-098** [tag: knowhow] [difficulty: medium]
  - **Q:** Why is `one.engine` a property that auto-selects instead of being set once at startup?
  - **A:** It returns whichever engine matches the current runtime; cached internally so the selection is cheap but the `One` class doesn't need a separate `init_for_local()` / `init_for_vercel()` method.
  - **Source:** `smb_loan_ai/one/one_02_db.py`

- **Q-099** [tag: knowhow] [difficulty: hard]
  - **Q:** A reviewer says "this should be a Bedrock Agent instead of a Strands agent." What's the strongest pushback?
  - **A:** Bedrock Agents require AWS-side configuration (action groups, function schemas, knowledge bases) that lives outside the repo. Strands keeps the agent definition entirely in code — easier to version, test, and review in PRs.
  - **Source:** `docs/learn-this-project/01-knowhow-inventory.md#external-dependencies--the-non-obvious-ones`

- **Q-100** [tag: runbook] [difficulty: medium]
  - **Q:** You changed the system prompt and your test agent still gives the old behavior. What's the diagnosis?
  - **A:** The prompt is `@cached_property` — loaded once per process. Restart the FastAPI process (or your `test_agent.py` Python invocation) to pick up the new prompt.
  - **Source:** `smb_loan_ai/one/one_04_agent.py`
